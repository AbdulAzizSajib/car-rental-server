enum UserRole {
    USER
    ADMIN
    DRIVER
    HOST
}

enum BookingStatus {
    PENDING
    CONFIRMED
    ONGOING
    COMPLETED
    CANCELLED
}

enum PaymentStatus {
    PENDING
    PAID
    FAILED
    REFUNDED
    CANCELLED
}

enum PaymentMethod {
    BKASH
    NAGAD
    STRIPE
    CASH_ON_DELIVERY
    BANK_TRANSFER
}

enum TransmissionType {
    MANUAL
    AUTOMATIC
}

enum FuelType {
    PETROL
    DIESEL
    ELECTRIC
    HYBRID
}

enum DiscountType {
    PERCENTAGE
    FIXED
}

enum BodyType {
    Sedan
    Wagon
    Coupe
    Hatchback
    Pickup
    Sport_coupe
    Crossover
    Van
}

enum VerificationStatus {
    PENDING
    APPROVED
    REJECTED
}

enum DriverStatus {
    PENDING
    APPROVED
    REJECTED
    SUSPENDED
}
