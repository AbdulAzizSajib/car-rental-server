enum UserRole {
    USER
    ADMIN
    DRIVER
    HOST
}

enum BookingStatus {
    PENDING
    CONFIRMED
    ONGOING
    COMPLETED
    CANCELLED
}

enum PaymentStatus {
    PENDING
    PAID
    FAILED
    REFUNDED
    CANCELLED
}

enum PaymentMethod {
    BKASH
    NAGAD
    STRIPE
    CASH_ON_DELIVERY
    BANK_TRANSFER
}

enum TransmissionType {
    MANUAL
    AUTOMATIC
}

enum DiscountType {
    PERCENTAGE
    FIXED
}

enum RentalType {
    ANY
    PER_DAY
    PER_HOUR
}

enum VerificationStatus {
    PENDING
    APPROVED
    REJECTED
}

enum DriverStatus {
    PENDING
    APPROVED
    REJECTED
    SUSPENDED
}

enum TripType {
    ONE_WAY
    ROUND_TRIP
}
