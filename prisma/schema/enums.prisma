enum UserRole {
    USER
    ADMIN
    DRIVER
    HOST
}

enum BookingStatus {
    PENDING
    CONFIRMED
    ONGOING
    COMPLETED
    CANCELLED
}

enum PaymentStatus {
    PENDING
    PAID
    FAILED
    REFUNDED
}

enum TransmissionType {
    MANUAL
    AUTOMATIC
}

enum FuelType {
    PETROL
    DIESEL
    ELECTRIC
    HYBRID
}

enum DiscountType {
    PERCENTAGE
    FIXED
}
